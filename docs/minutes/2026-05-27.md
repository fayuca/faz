# Minutes — 2026-05-27

---

## wrap — session end

### DESIGN

- Post-MVP roadmap reframed: P0 CRUD + `/api/v1`, P1 shell (planner + explorer placeholder), P2a–c explorer platform, P3 repetitive slices, P4–5 planner depth.
- Versioning: path `v1`, manifest contract, app semver in meta (`0.1.0` via Vite `__FAZ_VERSION__`).
- Session artifacts: roadmap **prose** (OVERVIEW) vs **checkboxes** (CHECKLIST) vs volatile **TASK**; manager may rename OVERVIEW → ROADMAP.

### META

- Gitignore: `.cursor/`, `docs/session/`, `docs/HANDOFF.md`, `docs/minutes/`.
- Task mode: `step` (elaboration) / `do` (agent implements); TASK format = checklist + **This step** only.
- Manager reports (interim): append to `double-click/docs/session/DRAFT.md`; spec base sent for future `/report` command.
- P1 built in task mode (user-led steps); agent commits `19dda4e`, `b97702f`, `00d4177`.

### CODE

- P0: update/delete UI, `ApiPaths` `/api/v1/transactions`, Render health check.
- P1: `shell/`, `apps/planner`, `apps/explorer`, `App.tsx` switcher, app version in meta.

### ART

- (none)

---

## reconcile — `.team/` layout (manager)

Manager migrated faz to current double-click scaffold. Event report in `.team/HANDOFF.md`. Not a deployment. **`docs/minutes/` now tracked** (removed from gitignore). User: **`/boot` → `/review`** in faz workspace.
