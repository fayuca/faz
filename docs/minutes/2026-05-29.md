# Minutes — 2026-05-29

---

## wrap — chat refresh (session continues)

### DESIGN

- P2a explorer UI baseline shipped in code: shared `ui/`, request views (read/write/id), chrome (ResourceList, VerbBar, ResponsePanel), static `baseline.ts`.
- Planner aligned: `views/` + `components/` + `hooks/` under `apps/planner/`; shared `api/` + `types/` at root.
- Per-app themes via `shell/themes.css` (meta header + app background); planner teal, explorer purple (muted).
- Transaction list criteria: `from` / `to` date filters wired in planner hook + explorer read view (backend `TransactionCriteria` already supported).

### META

- **Task mode** dogfooded — boot seeds one slice in TASK; elaborate with marked snippets; user **do** implements **This step** only; push rest to **Later**. Manager report pending: UC TASK MODE (session end).
- Mid-session **wrap** for chat refresh — not session end; TASK kept (P2b GET list).
- Pitfall logged: “let’s begin” after seed offer = seed only, not implement; “missing wiring” ≠ disable UI.

### CODE

- Frontend: `frontend/src/ui/`, explorer `views/` + `components/`, planner reorg + styled panels, `faz-form` spacing, blank favicon, removed Vite starter `icons.svg`.
- P2b not started (GET list Send → `getTransactions` → ResponsePanel).
- Dev expectation: Vite `:5173` + Spring `:8080` both up.

### ART

- (none)

---

## session wrap — P2b explorer bridge (session end)

### DESIGN

- **P2b** largely complete: all transaction verbs wired in explorer (GET list, GET by id, POST, PUT, DELETE) against shared contract; planner still uses root `api/` on v1 only.
- **Zod** client validation aligned with Jakarta `ValidationMessages` (user-facing copy); explorer uses `explorerApi.ts` with versioned paths.
- **Version UX:** per-request **v1** / **v2** toggle buttons on `RequestPathRow` (not header badge); `baseline.versions` drives visibility — add `"v2"` when backend exists. **P2c** manifest next.
- Deploy is separate session; Render live URL verified (free-tier cold start ~15–30s).

### META

- **Task mode** refined: long elaborations → **LESSON.md**; “let’s begin” = workflow start, not **do**; SCM gate held until **/wrap** (no auto-commit: tree dirty at session start).
- Infra LESSON §6–10 refreshed (Compose + Render in repo; not “files to add”).
- Manager report still pending: UC **TASK MODE**.

### CODE

- Explorer: `useExplorerRequest`, `explorerApi`, `RequestPathRow`, version state, client errors on Request (`explorer__request-error`), Response `<pre>` for HTTP only.
- GET: List | By ID toggle; sort property/direction combos; direction resets to `asc` when sort cleared.
- Backend: `ValidationMessages`, `HttpMessageNotReadableException` → `fieldErrors.date`, `ApiInfo.notFound` user-facing.
- `getTransaction` + `HttpResult` on delete (204). `zod` + `zod-validation-error` in frontend.

### ART

- Discriminated union pattern (`parseTransactionId` ok/message) for type-safe client errors.

---

## 19:00 — session wrap (P2c + API errors)

### DESIGN

- **P2c done:** backend **`GET /api/v1/manifest`** (`ApiManifest*`, operations[]); explorer loads manifest at runtime; `baseline.ts` removed. Backend is manifest authority; explorer/planner are API consumers.
- **API-only backend:** all errors → JSON `ApiError`; **`/` → Swagger UI**; springdoc **Manifest** + **Transactions** tags.
- **P3 direction:** raw JSON + **schema-synthesized placeholders** (not hand `example` blocks); audit `@Schema`/validation metadata for stack alignment.
- v2 mock dropped; version buttons from manifest when backend lists versions.

### META

- Chat refresh wrap; **`cleanAtStart` = no** — no auto-commit. **CHECKLIST:** P2c ticked.
- **TASK** reseeded for P3 schema slice; offer user recommended work at next boot.

### CODE

- Backend: `ApiManifestController/Service`, DTOs `dto/api/`, `RootController`, `ApiErrorIntegrationTest`, `GlobalExceptionHandler` extended, `ApiPaths.V1_MANIFEST`.
- Frontend: `api/manifest.ts`, `api/paths.ts`, shared `api/transactions.ts` with scope; explorer wired to manifest.
- Smoke-tested backend (manifest, errors, Swagger redirect).

### ART

- (none)

---

## 20:00 — session wrap (P3 slices 1–2 + slice 3 seed)

### DESIGN

- **P3.1 done:** manifest **`requestBody.schema`** on POST/PUT; explorer **`synthesizePlaceholder`** replaces hardcoded `BODY_SAMPLE`; metadata audit (`@Schema` on `TransactionRequest`, enum from `TransactionCategory`).
- **P3.2 done:** manifest **`queryParams`** / **`pathParams`**; generic **`ParamFields`**; GET list | by-id toggle unchanged; **minAmount/maxAmount** added to official query set; `@Schema` on `TransactionCriteria`.
- **P3.3 seeded:** real **`POST /api/v2/transactions`** (not mock); manifest version registry; default breaking delta = optional **`notes`**; per-version **`requestBody` map** recommended at elaborate.
- **Explorer user-readiness backlog** #1–6 captured in HANDOFF (sequential P3).

### META

- **Task mode:** boot → elaborate (snippets in TASK) → **do** per slice; user confirmed minAmount/maxAmount in slice 2.
- **`cleanAtStart` = no** — large pre-existing dirty tree; **no wrap auto-commit/push**.
- Dev servers **shut down** at user request (Vite `:5173`, Spring `:8080`).
- Pitfall: stale Spring JVM without new controllers → manifest 404 until restart.

### CODE

- Backend: `ApiOperation` extended (requestBody, queryParams, pathParams); `ApiManifestService` wired; `ApiManifestControllerTest` extended; `ValidationMessages`, API errors, manifest controller (P2c carry-over in tree).
- Frontend: `ParamFields`, `paramValues`, `synthesizePlaceholder`; explorer views refactored; `api/manifest.ts` types.
- **TASK** holds P3 slice 3 seed; **DRAFT** cleared.

### ART

- Manifest as single authority for explorer form shape (body + params), same pattern as slice 1.
