# Minutes — 2026-05-31

---

## wrap — pause (domain-hardening + explorer framework arc)

### ARC pivot

- **Explorer = full-stack framework app** for this arc; **planner deferred**.
- **Business domain (user sense):** contract flows backend → manifest/OpenAPI → clients; eliminate hardcoding; compile-time types required before calling stack done.
- **Sequence locked:** backend tightening ✅ → type generation + explorer finish → **v2/currency only then** (no reports / second resource now).
- Reports → [BACKLOG.md](../.team/session/BACKLOG.md).

### CODE (uncommitted)

**Backend — domain hardening (chunks 1–3)**

- Entity `nullable` + `@NotBlank` on description; criteria validation; `BindException` handler.
- [`ApiSchemaDefinitions.java`](../backend/src/main/java/systems/redtape/faz/manifest/ApiSchemaDefinitions.java); manifest `requestBody` per version map (`v1`, …).
- Test suite unified under [`backend/src/test/java/systems/redtape/faz/support/`](../backend/src/test/java/systems/redtape/faz/support/).

**Frontend — explorer framework (chunk 4)**

- [`validateManifestSchema.ts`](../frontend/src/apps/explorer/validateManifestSchema.ts), [`explorerMessages.ts`](../frontend/src/apps/explorer/explorerMessages.ts).
- Explorer decoupled from planner `transactionRequest` / `TransactionMessages`.
- [`requestBodySchemaForVersion`](../frontend/src/api/manifest.ts) for versioned body schemas.

### SESSION

- DOCKET seeded; fast task mode + file-link policy in HANDOFF.
- Manager event report (2026-05-31) acknowledged; `.cursor/` empty until reinstall.
- **Next TASK:** OpenAPI → TS codegen (DOCKET #5).

### LAND

- (none — DESIGN / ROADMAP / CHECKLIST not updated; arc lives in DOCKET + HANDOFF until user approves land)

---
