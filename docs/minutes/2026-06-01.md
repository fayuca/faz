# Minutes — 2026-06-01

---

## wrap — Explorer + planner UI arc complete; finishing arc queued

### Shipped (uncommitted)

**Backend**

- Rebrand `systems.redtape`; v2 `/api/v2/transactions`; `Currency` (USD, EUR)
- Manifest version-keyed `queryParams` / `requestBody`; criteria currency filter
- `ApiManifestVersionCoverage` test guard

**Frontend**

- OpenAPI → TS codegen; explorer manifest finish (GET form, coverage banner)
- Planner rebuild: flex filters · data table · pagination · submit box; planner on v2
- Shared UI scrollbars; table layout fixes

**Session**

- API version update checklist → HANDOFF
- Bookkeeping (GnuCash reference) assessed → BACKLOG; user wants it but **not** ad-hoc — design later
- DOCKET #1–7, #9 ✅; #8 deploy folded into **Finishing** arc

### LAND

- **Skipped** — user wrap without land. DESIGN still lists bookkeeping as non-goal; update when bookkeeping arc is approved. Finishing arc lives in DOCKET + HANDOFF only.

### Next

- **Finishing arc:** loose ends → QA → deployment ([DOCKET.md](../.team/session/DOCKET.md))
- Bookkeeping remains BACKLOG until dedicated design pass

---
