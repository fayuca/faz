# /review

Review **git changes** in this repo — summary only, no edits.

Run `git status` and diff staged + unstaged. Reply in **two groups**:

| Group | Content |
|-------|---------|
| **Staged** | Files and one-line summary per file (or "none") |
| **Unstaged** | Files and one-line summary per file (or "none") |

If not a git repo, say so and stop.

Do not fix, commit, or land — report only unless the user asks for more.
